<nav class="navbar navbar-expand-md bg-body-secondary sticky-top">
	<div class="container-fluid">
        <a class="navbar-brand" href="/">
            <% if(currentUser) { %>
                <% if(currentUser.isDemo) {%>
                    <span class="username">Guest Demo's</span> 
                <% } else { %>
                    <span class="username"><%= currentUser.username %>'s</span> 
                <% } %>
            <% } else { %>
            <span class="username">Guest's</span>
            <% } %> Personal Planner
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/calendar">Calendar</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/recipes">Recipe Archive</a>
            </li>
            <% if(currentUser && currentUser.isAdmin) {%>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/admin">Admin Portal</a>
                </li>
            <% } %>
            </ul>
            <div class="navbar-nav ms-auto d-flex">
                <!-- <button class="btn btn-primary">Push Updates</button>
                <button class="btn btn-warning">Pull Updates</button> -->
                <% if(!currentUser){ %>
                    <form class="me-2" action="/demo" method="POST" id="demo-form">
                        <button id="demo-button">Guest Demo</button>
                    </form>
                    <form class="me-2" action="/login">
                        <button id="login-button">Log In</button>
                    </form>
                    <form action="/signup">
                        <button id="signup-button">Sign Up</button>
                    </form>
                <% } else { %>
                    <form action="/logout">
                        <button id="logout-button">Log Out</button>
                    </form>
                <% } %>
            </div>
        </div>
      </div>
</nav>